const myData = {
    declarations: (pose, poseHistory) => [
        {'circle':{
            'x': pose.rightWrist.x,
            'y': pose.rightWrist.y,
            'd': 10
            }
        },
    ]
}
